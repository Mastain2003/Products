const items=PRODUCTS.flatMap(p=>p.variants.map(v=>({...v,parent:p}))),q=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])),money=(v,c='INR')=>new Intl.NumberFormat('en-IN',{style:'currency',currency:c,maximumFractionDigits:0}).format(v),grid=q('#grid'),search=q('#search'),brand=q('#brand'),availability=q('#availability'),condition=q('#condition'),sort=q('#sort');
[...new Set(items.map(x=>x.parent.brand))].sort().forEach(b=>brand.insertAdjacentHTML('beforeend',`<option value="${esc(b)}">${esc(b)}</option>`));
function render(){let text=search.value.toLowerCase().trim();let list=items.filter(x=>(!text||JSON.stringify(x).toLowerCase().includes(text))&&(!brand.value||x.parent.brand===brand.value)&&(!availability.value||x.availability===availability.value)&&(!condition.value||x.condition===condition.value));if(sort.value==='low')list.sort((a,b)=>(a.salePrice??a.price)-(b.salePrice??b.price));if(sort.value==='high')list.sort((a,b)=>(b.salePrice??b.price)-(a.salePrice??a.price));if(sort.value==='name')list.sort((a,b)=>a.title.localeCompare(b.title));q('#count').textContent=`${list.length} product variant${list.length===1?'':'s'}`;grid.innerHTML=list.map(x=>`<article class="card"><a class="card-image" href="product.html?id=${encodeURIComponent(x.id)}"><img src="${esc(x.images[0])}" alt="${esc(x.title)}" loading="lazy"></a><div class="card-body"><small>${esc(x.parent.brand)} · ${esc(x.parent.productTitle)}</small><h3><a href="product.html?id=${encodeURIComponent(x.id)}">${esc(x.title)}</a></h3><p class="variant">${Object.entries(x.attributes||{}).map(([k,v])=>`${esc(k)}: ${esc(v)}`).join(' · ')}</p><p class="availability">${esc(x.availability)}</p><div class="price"><strong>${x.salePrice?money(x.salePrice,x.currency):money(x.price,x.currency)}</strong>${x.salePrice?` <del>${money(x.price,x.currency)}</del>`:''}</div></div></article>`).join('');q('#empty').hidden=list.length!==0}
[search,brand,availability,condition,sort].forEach(x=>x.addEventListener('input',render));q('#clear').onclick=()=>{search.value=brand.value=availability.value=condition.value='';sort.value='featured';render()};q('#year').textContent=new Date().getFullYear();render();
